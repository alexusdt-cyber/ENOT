# 🚀 Настройка NoteFlow в Replit

## Пошаговая инструкция по запуску

### 1. Импорт проекта в Replit

1. Откройте [Replit](https://replit.com)
2. Нажмите "Create Repl"
3. Выберите "Import from GitHub" или загрузите файлы напрямую
4. Replit автоматически определит Node.js проект

### 2. Автоматическая установка

После импорта Replit автоматически:
- Установит все зависимости из `package.json`
- Настроит окружение с помощью `replit.nix`
- Сконфигурирует порты согласно `.replit`

### 3. Запуск приложения

Просто нажмите большую зеленую кнопку **"Run"** вверху экрана.

Приложение запустится на порту 5173 и откроется в браузере Replit.

### 4. Альтернативный запуск через Shell

Если нужно запустить вручную:

\`\`\`bash
# Установка зависимостей (если не установлены автоматически)
npm install

# Запуск dev сервера
npm run dev
\`\`\`

## ⚙️ Конфигурация

### Файлы конфигурации Replit

- **`.replit`** - основной конфиг для Replit
  - Команда запуска: `npm run dev`
  - Порты: 5173 (внутренний) → 80 (внешний)
  - Language server для TypeScript

- **`replit.nix`** - зависимости системного уровня
  - Node.js 20
  - TypeScript Language Server
  - Yarn и Jest

### Vite конфигурация

В `vite.config.ts` настроено:
- Host: `0.0.0.0` (для доступа извне)
- Port: `5173`
- Строгий порт выключен (для гибкости)

## 🔧 Решение проблем

### Проблема: Приложение не запускается

**Решение:**
\`\`\`bash
# Очистить кеш и переустановить зависимости
rm -rf node_modules package-lock.json
npm install
npm run dev
\`\`\`

### Проблема: Порт уже занят

**Решение:**
Vite автоматически найдет свободный порт. Проверьте в консоли какой порт используется.

### Проблема: TypeScript ошибки

**Решение:**
\`\`\`bash
# Проверить конфигурацию TypeScript
npx tsc --noEmit
\`\`\`

### Проблема: Стили не загружаются

**Решение:**
Убедитесь что `styles/globals.css` импортирован в `main.tsx`

## 📱 Доступ к приложению

После запуска вы получите URL вида:
\`\`\`
https://[your-repl-name].[your-username].repl.co
\`\`\`

Этот URL можно:
- Открыть в любом браузере
- Поделиться с другими пользователями
- Использовать для тестирования на мобильных устройствах

## 🔄 Автоматическая перезагрузка

Vite поддерживает Hot Module Replacement (HMR):
- Изменения в коде сразу отображаются в браузере
- Не нужно перезапускать сервер
- Состояние приложения сохраняется

## 🛠️ Команды разработки

\`\`\`bash
# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен сборки
npm run preview
\`\`\`

## 📦 Зависимости

Все необходимые пакеты указаны в `package.json`:

**Основные:**
- React 18.3.1
- TypeScript 5.4
- Vite 5.1.6
- Tailwind CSS 4.0

**UI Библиотеки:**
- Radix UI компоненты
- Lucide React (иконки)
- Motion/React (анимации)

**Дополнительные:**
- React DnD (drag & drop)
- PrismJS (подсветка кода)
- Sonner (уведомления)

## 🎨 Кастомизация

### Изменение цветовой схемы

Откройте `styles/globals.css` и измените CSS переменные:

\`\`\`css
:root {
  --primary: ...;
  --secondary: ...;
  /* и т.д. */
}
\`\`\`

### Изменение портов

Отредактируйте `.replit` и `vite.config.ts` для использования других портов.

## 🔐 Переменные окружения

Если нужны API ключи или секреты:

1. В Replit откройте вкладку "Secrets" (🔒)
2. Добавьте переменные
3. Используйте в коде через `import.meta.env.VITE_YOUR_SECRET`

## 📊 Мониторинг

В консоли Replit вы увидите:
- Логи сервера Vite
- Ошибки компиляции
- Предупреждения TypeScript
- Информацию о HMR

## 🚀 Готово!

Теперь ваш NoteFlow полностью готов к работе в Replit!

Приятной разработки! 💙
